import React from 'react';

const WanderInfoContext = React.createContext({});

export const WanderInfoProvider = WanderInfoContext.Provider;
export const WanderInfoConsumer = WanderInfoContext.Consumer;
